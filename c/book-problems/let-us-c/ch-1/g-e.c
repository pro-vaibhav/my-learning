#include <stdio.h>

int main(void)
{
    int l, b, area, perimeter;
    printf("Enter the length of the rectangle : ");
    scanf("%d", l);
    printf("Enter the breath of the rectangle :");
    scanf("%d", b);
    area = l * b;
    perimter = 2 * l + b;
    printf("area of the rectangle is :%d", area);
    printf("perimeter of the rectangle is :%d", perimter);
    return 0;
}